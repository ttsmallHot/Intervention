# Eval config — Qwen3-VL-4B + FrozenLake probe (VQA accuracy)
model_type : qwen3vl
model_path : /code/Qwen3-VL-4B-Instruct
data_path  : /code/data/ezpoints_probe_train/train.parquet

# Checkpoint to evaluate (trained plugin)
checkpoint : null

# Results output
output_dir : /code/Intervention/results/qwen3_ezpoints

# Eval options
max_samples   : null
fixed_strength: 0.3     # fallback strength if checkpoint is not found
